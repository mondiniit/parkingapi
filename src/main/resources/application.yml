spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mydb?useSSL=false
    username: root
    password: root1234
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      naming-strategy: org.spring.cfg.ImprovedNamingStrategy
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect